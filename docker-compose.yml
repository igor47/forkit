services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - app_data:/app/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

volumes:
  app_data:
    driver: local
